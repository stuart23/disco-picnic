# Populated by `terraform plan -generate-config-out=generated.tf` (see import.tf).
# After the first successful apply, move the generated `aws_s3_bucket.site`
# block (and any related aws_s3_bucket_* sub-resources: versioning,
# public_access_block, policy, website config, etc.) here.
